// Setup global de testes de componente (vitest + @testing-library/react).
// Carregado automaticamente antes de cada arquivo de teste (ver vite.config.js `test.setupFiles`).
import '@testing-library/jest-dom/vitest';
