## 1. UI Modal Nova Empresa

- [ ] 1.1 Adicionar os estados `dataInicio` e `dataFim` no componente `ModalNovaEmpresa` em `src/components/abertura-empresa.jsx`
- [ ] 1.2 Adicionar os dois inputs do tipo `date` no formulário do modal
- [ ] 1.3 Atualizar a função `handleSubmit` do `ModalNovaEmpresa` para repassar `dataInicio` e `dataFim` no handler `onSave`

## 2. Integração com Backend

- [ ] 2.1 Atualizar `handleCreateEmpresa` em `AberturaEmpresaPage` para repassar as datas para `empresasApi.criarEmpresa`
